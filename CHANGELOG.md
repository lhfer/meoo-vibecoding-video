# 2.0.1 · 2026-09-16 共创开源发布

- 新增 {一起 Vibe} 与秒悟的共创说明、安装指南和仓库展示。
- 修复 macOS /var 与 /private/var、目录符号链接造成的交付指纹计算错误；新增等价路径回归用例。
- 原始英文 Skill 名称和命令接口保持兼容。
- 品牌及案例媒体与 MIT 代码许可分别说明。

# 2.0.0 · 2026-09-12

上一版：1.0.5。

## 主要改动

- 重排执行顺序：初始化与宿主探测早于任何台账写入；增加只读式 resume，拒绝覆盖残缺工程。
- 缺素材默认自主获取；新增真实 Playwright 截图/操作录屏、收据、来源与素材映射。宿主能力按真实调用记录，未测试不写成功。
- 材料分开 acquired/ready/used；真实文件解码、来源、检查记录、哈希及镜头使用成为门禁。required 不能静默跳过，生成媒体不能满足 real-demo。
- SafeText、SafeBox、统一几何引擎、字体探测与每个实际渲染帧的 DOM LayoutGuard；标题/字幕最小字号接到实现。
- 替换两个存在固定坐标碰撞风险的参考镜头；移除示例中未经核实的真实公司新闻、社交帖与产品实测数字。模板标记不能进入正式片。
- QA硬错误非零退出；白色产品背景不直接当成闪白，分布与曝光候选保持审阅提示。
- 原分辨率关键帧、实际视听记录与交付门禁；修改后旧检查失效，响度后期需重新核对最终文件。
- 保留原有TTS/音乐/封面/来源、两种创作profile与用户确认机制。

## 迁移

`materials.py set --status ready` 改为实际查看后的 verify。`--strategy existing` 只表示已有候选，不自动就绪。旧的 captions.bottom/maxWidth 布局旋钮不再独立控制字幕区域，请改 film.style.layout[format] 并重新验收。旧自定义镜头须接入 SafeBox/SafeText 或等价 data-qa 标记；不自动升级旧工程。

## 能力边界

DOM几何检查不理解位图/视频里的文字或事实；CLI记录的视觉/试听是实际审阅声明，不是本地自动视觉模型。工作室安全边距不是平台官方标准。Meoo的工具授权、外网、登录和生成额度不能由技能提供。

补充：修复真实中文两行字形溢出；字幕底条自适应文本；提供声明式全屏背景；渲染逐帧回执覆盖与引用媒体的流式 hash；离线 HTML 分支明确为示意而非实机证据。
