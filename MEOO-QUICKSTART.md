# 两个视频 Skill 升级与 Meoo 使用说明

## 文件怎么选

- `article-to-vertical-video-v5.0.0.zip`：科技文章、新闻、观点、教程与案例视频，保留原资讯版的较快叙事与声音偏好。
- `vibecoding-launch-video-v2.0.0.zip`：产品/应用/游戏宣发，保留原宣发版的克制讲述，优先展示真实结果、操作与边界。

这是两个完整、独立的技能包，不是补丁，也不是上一轮诊断包。分别导入，不要把包含两个包的资料总 ZIP 当一个技能安装。

**验证状态：本地 Python、Node、Chromium 回归通过。Meoo 端到端和完整 Remotion 构建/渲染仍需在目标环境验收；不是“任何环境都已证明可用”的承诺。** 细节见随包 `VERIFICATION.md`。

## 这次改了什么

| 原问题 | 新实现 |
|---|---|
| 先建素材台账导致初始化失败 | 初始化先行，后续脚本拒绝半初始化工程；resume 不覆盖现有内容 |
| 文档参数与 CLI 不一致 | 对齐素材登记接口；现支持 add/set 的实际 asset/source 等参数 |
| 只写了截图要求，没有执行路径 | 宿主浏览器优先，补充可运行 Playwright 截图、受限操作录屏与采集回执 |
| 未取材/未看过也标 ready | pending → acquired → verified ready；核对文件、解码、hash、来源、使用依据、审阅与镜头声明 |
| 缺录屏就立刻让用户录 | 先尝试获授权的现有工具/真实入口/合适的一手素材，真实受阻才给最少补充请求 |
| 文字、主体、字幕各算各的坐标 | 同一几何引擎；真正测量字体换行；字号下限、越界/重叠检查 |
| 只用固定卡片模板 | 保留两版独立风格，允许显式声明的全屏电影感背景；默认技术例子不能当正式成片 |
| 截接触表就宣称检查完 | 渲染期 DOM 几何检查 + 全分辨率关键帧 + 实际视频/音频审阅，分别记录 |
| QA 命令运行成功被当成质量通过 | 硬失败非零；渲染检查要求逐帧回执，旧 QA 不能复用到新视频 |
| 改过工程/媒体却沿用旧验收 | 最终文件与实际引用媒体、代码、资料、QA/审阅记录绑定 hash |

## 导入与第一次运行

保留原 v4 / v1 ZIP 以便回滚。导入新版后用一个**新任务**验证，明确写技能名和版本，避免同名旧版混用。资讯和宣发按当前任务只选对应的一套。

让 Agent 首先展示：读到的 `SKILL.md` 实际路径、名称和版本，以及搜索、浏览器、截图/录屏、看图/试听、TTS/生图工具的实际能力状态。不要接受一个没有真实调用依据的“全部就绪”。

视频工程在独立 `<work>/project` 中建立，不能覆盖 Meoo 原有网页应用的 `src`、`package.json` 或锁文件。最后需要的是视频文件与工程，而不只是一个有播放按钮的网页。

首次建议先完成随包 `VERIFICATION.md` 的类型检查与技术草稿测试，确认宿主的运行依赖和浏览器链路确实能用，再用你的真实选题跑完整流程。技术草稿的测试图表、测试配音不属于作品素材。

启动提示词另见 `Meoo-video-skills-start.txt`；填写真实文章或产品入口即可，不必向 Agent 再解释整套新规则。

## 正常情况下会交付

真实素材台账与来源记录、完整工程、要求画幅的视频；对应机器 QA、实际视听记录与 `out/delivery.json`。封面、干声版、发布文案按当前 brief 的要求另外核验。没有提供的画幅不要擅自增加。

保留原方案卡、完整脚本、实际前十秒样片的协作节点；已明确确认的内容不重复问，但不能自动伪造用户对尚未看到的样片的批准。

## 三个需要明确的边界

**Skill 不会凭文字创造权限。** Meoo 没开放浏览器/联网/试听，或依赖无法安装时，应返回真实日志、当前工程和待检草稿，而不是悄悄做成通用文字卡片。

**本地 HTML 截图是示意/技术测试，不是真实网站访问。** 新增的离线 HTML 分支会记录 `sourceMode=local-html`，不能用于通过产品实测门禁。官网、产品实机录屏仍须真实取得并实际查看。

**几何正确不等于片子好看。** 检查器主要处理字号、布局和交付一致性，不理解素材内部内容与人类观看感受。还需要实际看大图、播放成片、听声音。

## 老工程怎么处理

上传新版技能不会自动替换旧工程里的组件。最稳妥的方式是在副本/新工程中移入已确认脚本与合法素材，再迁移镜头组件到 SafeText/SafeBox/useFrameLayout。不要让 Agent 对旧工程强制初始化或无差别覆盖。

字体、共享组件、布局或声音发生变化，旧开头批准可能失效；只有满足原有解码比对条件时才能继承。最终视频更改后，机器 QA 和视听验收需要跟随最终文件重新确认。

## 提供失败信息时

保留完整报错、能力探测回执、`content/film.json`、素材台账、失败画面和对应镜头源码。这样能区分“工具根本没有/权限受阻”“程序没运行”“素材确实缺失”和“真实布局缺陷”，不再靠反复加形容词修复。
