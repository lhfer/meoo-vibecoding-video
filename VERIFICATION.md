# V5 / V2 本地验证记录

验证日期：2026-09-12。状态：**本地逻辑与浏览器回归通过；完整 Remotion 构建/渲染及用户 Meoo 端到端尚未验证。**

## 两个独立包均已执行

| 项目 | 每个包的结果 | 证明范围 |
|---|---|---|
| Python unittest | 105 项：103 通过，2 跳过，0 失败 | 原功能回归 + 素材状态、初始化保护、媒体/来源、交付指纹、采集权限、关键帧、QA 退出码等 |
| Node tests | 22 项通过 | 原时间轴编译 + 三画幅几何、自定义边界、渲染回执完整性契约 |
| 实际 Chromium 检查 | 26 项通过 | 使用随包 DOM/布局模块逻辑测量中文换行、字幕碰撞、英文长词、缩放、动画边界、全屏背景层；实际本地 HTML 截图与点击录屏；字体探测 |
| TypeScript 语法转译 | 25 个源文件通过 | 仅语法，不是安装依赖后的 `tsc --noEmit` 类型检查 |

两个跳过项来自原仓库 `tests/test_package.py`，需要源仓库的 `profiles/`；独立技能包不包含该目录。最终 ZIP 另行执行真实打包、CRC、目录/入口/依赖锁一致性、凭据与字体二进制排除检查。

## 新增回归覆盖

缺少或全为可选项的素材计划不会通过；`existing` 不自动 ready；必需项不可静默 skipped；`add --asset` 真正可用；HTML 冒充图片、缺文件/来源/使用依据、过期 hash 和未查看文件均被拒绝；截图/生成画面不能冒充实机视频；必需素材需绑定镜头；未初始化不能先写台账；`--resume` 不覆盖旧工程；更改组件、渲染核心或引用媒体会使交付指纹失效；QA 硬失败不再返回成功。

浏览器回归期间发现并修复了中文两行字形超出行框的问题。字号下限没有降低，而是把实际滚动溢出加入适配判定，并增加标题/字幕的字形余量。字幕背景改为随文字的短条，不再铺满一个空的大黑框。

## 本次没有证明的内容

- `npm ci` 在本环境遇到 npm registry DNS `EAI_AGAIN`，没有完成依赖安装。锁定 Remotion 版本保留为 4.0.520；因此没有声称完整 React 类型检查、Webpack 构建或 Remotion 视频渲染通过。
- 系统 Chromium 对本地 HTTP 导航返回 `ERR_BLOCKED_BY_ADMINISTRATOR`。未修改浏览器安全策略；布局测试以浏览器内 data-URL 模块加载同一套逻辑，采集测试使用明确标注的自包含本地 HTML。**这不是实际官网联网采集测试。**
- 录屏测试在临时的测试专用 Playwright 缓存中使用系统 FFmpeg，没有改动全局驱动；Meoo/常规宿主应使用获授权的宿主浏览器或正常安装的 Playwright Chromium/FFmpeg。
- 没有进入用户的 Meoo 项目，没有检查真实工具权限，也没有调用用户的付费 TTS、图片或视频账户。Grok/Fish 等原适配器被保留，但本次不包含在线生成能力验证。
- 图片内部字体、真实素材语义、音画意义、风格、节奏、隐私和授权依据仍需要实际视听/人工判断。CLI 审阅记录是签注，不是“机器真的看懂了”的证明。

## 在目标宿主完成最后一段验收

```sh
# 技能根目录：纯逻辑回归
python3 -m unittest discover -s tests -p 'test_*.py' -v
node --test tests/*.test.mjs

# 安装模板运行依赖，再做真实类型检查（需要目标宿主网络/安装许可）
cd assets/director
npm ci
npm run check
cd ../..

# 此用例生成明确标注的技术草稿 + 合成音频，不伪造用户批准或产品证据
python3 tests/render_smoke.py --workdir /absolute/fresh-test-workdir --scale 0.5
```

`tests/browser_regression.py` 的测试专用系统 FFmpeg 缓存适配目前针对 Linux；实际 `scripts/capture.py` 不依赖此测试适配器。Meoo 没有允许这些操作时，应报告能力/权限阻塞，而不是假造测试结果。

测试截图和短视频只用于验证程序，不是真实产品演示，不应作为用户作品素材发布。包内不分发任何字体文件、账户凭据、node_modules 或渲染产物。
