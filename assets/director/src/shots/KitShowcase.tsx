/** No fictional company claims or social posts in the default template. Author a bespoke evidence-led scene. */
import type {ShotProps} from '../types';
import EvidenceScene from './EvidenceScene';
export default function KitShowcase(props:ShotProps){return <EvidenceScene {...props} mode="news"/>;}
