/** A safe reference only. Real launch films must replace the placeholder with an inspected real product recording. */
import type {ShotProps} from '../types';
import EvidenceScene from './EvidenceScene';
export default function ProductShowcase(props:ShotProps){return <EvidenceScene {...props} mode="product"/>;}
