# 2.0.1 发布验证

日期：2026-09-16。平台：macOS，Python 3.14，Node.js，锁定 Remotion 4.0.520。

| 检查 | 实际结果 | 证明范围 |
|---|---|---|
| Python 回归 | 106 项：104 通过、2 跳过、0 失败 | 脚本行为、路径、素材与交付校验 |
| Node 回归 | 22 项通过 | 时间轴、布局计算和帧回执 |
| npm ci + TypeScript | 通过 | 锁定依赖可安装，模板类型检查通过 |
| 实际渲染 | 3:4、9:16、16:9 各 12 秒、30fps、半分辨率 | 各 360 帧布局回执完整，合成测试音轨在预期位置出现 |
| 仓库横幅 | 5.6 秒 GIF，约 1.26 MB | 首帧、流程中间态和结果态已查看 |
| 分享预览 | 1280×640 PNG，低于 1 MB | GitHub 分享图格式 |

两个跳过项要求原母仓库的 profiles/，独立分发包不包含该目录；独立 Release 会另外真实打包并检查 CRC、入口和依赖锁。渲染测试使用明确标记的合成音调与技术草稿，不能代表观众质量或真实产品实测。

本次修复了 /var 与 /private/var 以及目录符号链接引起的交付指纹错误，并加入回归测试。第一次使用 0.35 缩放时，Remotion 因浮点尺寸不是整数而拒绝；随后使用半分辨率完成三画幅渲染，保留原测试记录。

Meoo 账号当前生成额度不足，未进行新的在线端到端生成。已有项目的文件与能力记录正在用于视频证据核验；不把静态说明或历史完成文字当成新版技能的在线验收。主观配音听感和完整作品质量需在具体任务中确认。

旧版 2026-09-12 的记录保留在 [VERIFICATION.md](../VERIFICATION.md)。

## GitHub 发布后补验

- 公开仓库与 Release 资产已创建，GitHub 返回的 SHA-256 与本地发布 ZIP 一致。
- 从独立 ZIP 解压到临时目录，项目级安装成功；重复安装保留已有用户文件。
- 两库真实 GitHub 页面在 390px 手机宽度、浅色与深色主题下核对：图片全部加载，页面无横向溢出；桌面正文截图也已检查。
